# cedricpdf
First create a file "uploads" where the pdf files will be stored.

In the server.php at "if (isset($_POST['apply']))" are my queries for posting data
you will see how i have used the method

happy hacking!!!